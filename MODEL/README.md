## Model Artifacts

This folder contains the trained machine learning pipeline used for
financial fraud detection.

The model is saved using `joblib` and can be reused for prediction
without retraining.
